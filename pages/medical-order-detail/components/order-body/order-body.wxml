<view  class="order-detail_body">
  <good
    id="{{ order.id }}"
    name="{{ order.good.name }}"
    price="{{ order.good.price }}"
    logoUrl="{{ order.good.logoUrl }}"
    amount="{{ order.count }}"
  />
  <kvs items = "{{ kvs }}" />
</view>
