<view  class="order-detail_body">
  <good
    id="{{ order.id }}"
    name="{{ order.good.name }}"
    price="{{ order.good.price }}"
    logoUrl="{{ order.good.menuLogoUrl }}"
    amount="{{ order.amount }}"
  />
  <kvs items = "{{ kvs }}" />
</view>
