<!--logs.wxml-->
<view class="my-address">
  <addresses
    addresses="{{ addresses }}"
    selectedAddressId="{{ selectedAddressId }}"
    bind:addressChange="addressChange"
    bind:delete="deleteAddress"
  />
  <view
    class="button button--disabled"
    bindtap="goCreateAddress">添加新地址</view>
</view>
