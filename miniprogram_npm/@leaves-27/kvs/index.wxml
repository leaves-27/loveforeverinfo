<view class="order">
    <view class="order-item" wx:for="{{ items }}" wx:key="{{ index }}">
        <view class="order-item_key">
            {{ item.key }}
        </view>
        <view class="order-item_value">
            {{ item.value }}
        </view>
    </view>
</view>
